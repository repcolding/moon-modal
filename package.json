{
  "name": "@verno.digital/moon-modal",
  "version": "2.0.5",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild src/index.ts --outfile=dist/index.js --bundle --format=esm",
    "postbuild": "tsc --project tsconfig.build.json"
  },
  "keywords": [
    "@verno/module",
    "@verno.digital"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/repcolding/moon-modal.git"
  },
  "author": "telegramm: @repcolding; site: https://verno.digital",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "esbuild": "^0.16.12",
    "tsc": "^2.0.4",
    "typescript": "^5.0.4"
  },
  "private": false,
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
